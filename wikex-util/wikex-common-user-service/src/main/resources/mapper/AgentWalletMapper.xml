<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wikex.wikex.user.mapper.AgentWalletMapper">

    <update id="increaseBalance">
        update agent_wallet a set a.balance=a.balance + #{amount} where a.id = #{id}
    </update>
    <update id="decreaseBalance">
        update agent_wallet a set a.balance=a.balance - #{amount} where a.id = #{id}
    </update>
    <update id="setAgentWalletBalance">
        update agent_wallet a set a.balance= #{amount} where a.id = #{id}
    </update>
</mapper>
