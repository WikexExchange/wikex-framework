<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wikex.wikex.user.mapper.MemberMapper">

    <select id="findSuperPartnerMembersByIds" resultType="com.wikex.wikex.user.entity.Member">
        select * from member where id in (#{ids}) order by id desc
    </select>
    <select id="queryFirstAndSecondById" resultType="com.wikex.wikex.user.entity.Member">
        select * from  member
        where inviter_id = #{id}
           or inviter_id in (select id from member where inviter_id=#{id})
        order by id desc
    </select>
    <select id="getRegistrationNum" resultType="java.lang.Integer">
        select count(id) from member where date_format(registration_time,'%Y-%m-%d') = #{dateStr}
    </select>
    <select id="getBussinessNum" resultType="java.lang.Integer">
        select count(id) from member where date_format(certified_business_check_time,'%Y-%m-%d') = #{dateStr}
    </select>
    <select id="getApplicationNum" resultType="java.lang.Integer">
        select count(a.id) from member a , member_application b
        where a.id = b.member_id and b.audit_status = 2 and date_format(b.update_time,'%Y-%m-%d') = #{dateStr}
    </select>
    <select id="getStartRegistrationDate" resultType="java.util.Date">
        select min(a.registration_time) as date from member a
    </select>
</mapper>
