spring:
  application:
    name: wikex-permission
  cloud:
    nacos:
      config:
        file-extension: yaml
        server-addr: ${nacos.server-addr}
        username: ${nacos.username}
        password: ${nacos.password}
        auto-refresh: true 
        shared-configs: wikex-base.yaml,wikex-seata.yaml
      discovery:
        
        server-addr: ${nacos.server-addr}
        username: ${nacos.username}
        password: ${nacos.password}
