<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wikex.wikex.swap.mapper.TradingTimesMapper">

    <select id="findAll" resultType="com.wikex.wikex.swap.entity.TradingTimes">
        select t.*,c.symbol from trading_times t
        left join contract_coin c on c.id = t.contract_coin_id
        order by t.contract_coin_id asc
    </select>
</mapper>
